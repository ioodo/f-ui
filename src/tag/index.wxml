<f-button
  size="{{ size }}"
  type="{{ type }}"
  shape="{{ shape }}"
  icon="{{ icon }}"
  fill="{{ checked }}"
  text="{{ text }}"
  extClass="{{ extClass }}"
  block="{{ block }}"
  bindftap="onTap"></f-button>